import preprocessing

# test code to verify correctness
def test_sy():
    param = ['Bitmap Scan', 'Hash Join', 'Nested Loop Join', 'Hashed Aggregation', 'Materialization', 'Explicit Sort']
    preprocessing.permutation()


test_sy()